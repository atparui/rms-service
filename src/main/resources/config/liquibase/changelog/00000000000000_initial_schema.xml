<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd
                        http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

    <!--
        RMS initial schema - JHipster tables removed.
        
        User management is handled via:
        - rms_user table (synced from Keycloak)
        - user_branch_role table (app-specific roles: MANAGER, WAITER, CHEF)
        
        No JHipster tables (jhi_user, jhi_authority, jhi_user_authority) are needed.
    -->
    <changeSet id="00000000000001" author="rms-team">
        <comment>Initial schema placeholder - entity tables created in individual changesets</comment>
        
        <!-- This changeset is intentionally empty. All tables are created in their respective entity changesets. -->
        <!-- Keeping this file for Liquibase compatibility (initial schema ID) -->
    </changeSet>

</databaseChangeLog>

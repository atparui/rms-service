<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd
                        http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

    <!--
        Migrate existing role values in user_branch_role to match restaurant_role format.
        This removes the ROLE_ prefix and maps to standard role names.
        Must run BEFORE the FK constraint is added.
    -->
    <changeSet id="20260205115000-1" author="rms-team">
        <preConditions onFail="MARK_RAN">
            <tableExists tableName="user_branch_role"/>
            <columnExists tableName="user_branch_role" columnName="role"/>
        </preConditions>
        <comment>Ensure user_branch_role.role values have ROLE_ prefix to match restaurant_role.name format</comment>
        
        <!-- Ensure ROLE_ prefix is present -->
        <sql>
            -- Add ROLE_ prefix if missing (handle both old formats)
            UPDATE user_branch_role SET role = 'ROLE_MANAGER' WHERE role IN ('MANAGER', 'ROLE_MANAGER');
            UPDATE user_branch_role SET role = 'ROLE_CHEF' WHERE role IN ('CHEF', 'ROLE_CHEF');
            UPDATE user_branch_role SET role = 'ROLE_WAITER' WHERE role IN ('WAITER', 'ROLE_WAITER', 'ROLE_RUNNER', 'ROLE_SUPPORT');
            UPDATE user_branch_role SET role = 'ROLE_BARTENDER' WHERE role IN ('BARTENDER', 'ROLE_BARTENDER');
            UPDATE user_branch_role SET role = 'ROLE_CASHIER' WHERE role IN ('CASHIER', 'ROLE_CASHIER');
            UPDATE user_branch_role SET role = 'ROLE_HOST' WHERE role IN ('HOST', 'ROLE_HOST');
            UPDATE user_branch_role SET role = 'ROLE_DELIVERY_DRIVER' WHERE role IN ('DELIVERY_DRIVER', 'ROLE_DELIVERY_DRIVER', 'ROLE_BAKER');
        </sql>
    </changeSet>

    <!-- Changeset 2 removed - no longer needed as all roles are now in the main seed -->

</databaseChangeLog>
